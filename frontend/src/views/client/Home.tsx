import * as React from "react";
import Layout from "../../components/client/Layout";
interface HomeProps {}

const Home: React.FunctionComponent<HomeProps> = () => {
  return <Layout>asddasds</Layout>;
};

export default Home;
